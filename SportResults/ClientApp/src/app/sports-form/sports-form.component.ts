import { Component, Inject } from '@angular/core';
import { HttpClient } from '@angular/common/http';

@Component({
    selector: 'app-sports-form',
    templateUrl: './sports-form.component.html',
})
/** sports-form component*/
export class SportsFormComponent {
  //public sportform: SportForm[]

  //constructor(http: HttpClient, @Inject('BASE_URL') baseUrl: string) {
  //  http.get<SportForm[]>(baseUrl + 'api/SampleData/SportForm').subscribe(result => {
  //    this.sportform = result;
  //  }, error => console.error(error));
  //}
}


//interface SportForm {
 
//}
